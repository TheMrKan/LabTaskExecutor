[project]
name = "Lab"
version = "0.1.0"
description = "Add your description here"
requires-python = "==3.14"
dependencies = [
    "pytest>=8.4.2",
]

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
]
